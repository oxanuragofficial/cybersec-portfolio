# 🔍 Port Scanner in C

This project is a basic TCP port scanner written in C. It scans ports 1–1024 on a target IP and reports which ports are open.

## 🔧 Features

- Uses raw sockets
- Scans common ports
- Displays open ports in terminal

## 🧪 How to Run

```bash
gcc port_scanner.c -o scanner
./scanner
